import React from 'react';
import Icon from 'react-native-vector-icons/Ionicons';

const TabBarIconHome = ({size, color}: {size: number; color: string}) => (
  <Icon name="home" size={size} color={color} />
);

export default TabBarIconHome;
